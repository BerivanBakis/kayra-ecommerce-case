import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>Products App Çalışıyor 🛍️</h1>;

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<App />);
